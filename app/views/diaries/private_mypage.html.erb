<% content_for(:title, t('.title')) %>

<div class="ml-64 w-full p-6">
  <%= render "diaries/mypage_header" %>
</div>
  <div class="mt-16 p-4">
   <% if @diaries.present? %>
          <%= render @diaries %>
        <% else %>
          <div>掲示板がありません</div>
        <% end %>
  </div>