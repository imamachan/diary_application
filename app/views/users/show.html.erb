<% content_for(:title, t('.title')) %>

<div class="ml-64 w-full p-6">
  <%= render "users/mypage_header" %>
</div>
  <div class="mt-16 p-4">
   <div>
    <% if current_user.diaries.present? %>
    <%= render partial: "diaries/diary", collection: current_user.diaries, as: :diary %>
  <% else %>
    <div class="text-gray-600 mt-4">日記がありません</div>
  <% end %>
</div>
  </div>